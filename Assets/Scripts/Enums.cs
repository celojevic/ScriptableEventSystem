
public enum BoolOp
{
    True, False, Switch
}

public enum KeyPressType
{
    Down, Up, Hold
}

public enum MathOp
{
    Add, Sub, Div, Mul
}

public enum StartType
{
    Awake,
    OnEnable,
    Start
}